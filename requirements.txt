# Flask web framework
Flask==2.3.3

# For building REST APIs
Flask-Cors==4.0.0

# For parsing natural language
openai==1.3.7

# For handling environment variables (API keys)
python-dotenv==1.0.0

# Optional: For DB interaction (SQLite / MySQL / PostgreSQL)
SQLAlchemy==2.0.30

# Optional: For embedding & querying large language models
langchain==0.1.13
faiss-cpu==1.8.0

# Optional NLP tools
transformers==4.40.2
torch==2.1.2

# For parsing and visualizing code syntax (if needed)
pygments==2.17.2

# Optional utilities
requests==2.31.0
tqdm==4.66.2
